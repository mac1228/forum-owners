<div class="container content">
	<div class="row">
		<% home.forEach(function(item){ %>
			<div class="col-md-4">
				<a href="<%= item.url %>">
	  				<div class="homepagebox">
	  					<h2><%= item.name %></h2>
	  				</div>
	  			</a>
	  		</div>
		<% }); %>
	</div>
</div>